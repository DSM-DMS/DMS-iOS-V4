# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DMS_SwiftUI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DMS_SwiftUI
  pod 'CryptoSwift'
  pod 'RxAlamofire'
  pod 'RxSwift'

  target 'DMS_SwiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DMS_SwiftUIUITests' do
    # Pods for testing
  end

end
